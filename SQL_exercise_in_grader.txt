SELECT postal_code, COUNT(postal_code) 
FROM CUSTOMER 
GROUP BY postal_code;

SELECT postal_code, COUNT(postal_code) as sumpost
FROM CUSTOMER
GROUP BY postal_code
HAVING sumpost > 1;

SELECT * 
FROM ORDERT
WHERE CUSTOMER_ID = 10001;







CREATE VIEW customer_orders AS 
SELECT C.customer_id, C.customer_name, count(order_id) AS sum_orders
FROM CUSTOMER C, ORDERT O
WHERE C.customer_id = O.customer_id
GROUP BY C.customer_id;

SELECT customer_id, customer_name 
FROM customer_orders
WHERE sum_orders = (SELECT MAX(sum_orders) FROM customer_orders)ซ;
